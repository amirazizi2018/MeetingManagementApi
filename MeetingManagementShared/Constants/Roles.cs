using MeetingManagementShared.Enums;

namespace MeetingManagementShared.Constants;

public static class Roles
{
    public const string Admin = nameof(UserRole.Admin);
    public const string Member = nameof(UserRole.Member);
}
